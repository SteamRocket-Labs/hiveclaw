name: finance-research
cases:
- name: provider_status_only
  input: Check finance provider readiness.
  expected_tools:
  - finance_get_provider_status
  expected_artifacts:
  - finance-research-artifact
  - quality notes
- name: handles_missing_or_unsafe_inputs
  input: Run finance-research with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content

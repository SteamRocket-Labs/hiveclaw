cases:
- name: basic_docx_report
  input: Create a two-section DOCX report.
  expected_artifacts:
  - report.docx
- name: handles_missing_or_unsafe_inputs
  input: Run docx-generator with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: docx-generator

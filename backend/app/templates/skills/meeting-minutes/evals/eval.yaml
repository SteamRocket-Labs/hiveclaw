cases:
- name: meeting_minutes_markdown
  input: Extract minutes from a supplied transcript.
  expected_artifacts:
  - meeting-minutes.md
- name: handles_missing_or_unsafe_inputs
  input: Run meeting-minutes with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: meeting-minutes

cases:
- name: basic_workbook
  input: Create an XLSX workbook with inputs and formulas.
  expected_artifacts:
  - model.xlsx
- name: handles_missing_or_unsafe_inputs
  input: Run xlsx-processor with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: xlsx-processor

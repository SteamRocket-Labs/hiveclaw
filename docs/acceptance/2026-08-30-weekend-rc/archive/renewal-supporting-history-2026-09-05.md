---
document_id: weekend-rc-renewal-supporting-history-2026-09-05
owner: Codex
status: archive
authority: historical-only
last_reviewed: 2026-09-05
source_commit: 0ce51f049e03c689a440075a5de8a7a9d99c609c
verification_status: historical-supporting-evidence-not-current
---

# Renewal 历史 supporting evidence

[返回历史索引](README.md) · [当前唯一状态](../03-current-status.md)

以下原文从当前状态文档的历史区完整迁移，保留原有过程和证据引用。文中的“当前”“运行中”“下一步”仅指当时快照；“本文件”和“上表”分别指迁移前的 `03-current-status.md` 与其当时上方表格，不把本归档升级为当前状态源。过时的权限、任务状态、hash、测试或部署结果不作为当前执行授权、review 或 Journey PASS。当前事实与下一动作只读上方的唯一状态入口。

### 已结束的历史过程（仅对应快照证据）

- CC PDEC-013首次审查已读完；Codex随后独立查`security.py`/request header、`permissions`/resource authority、Personal/Company KB authority及ControlPlane。确认既有公司切换与角色真实来源可复用；同时反证CC的“直达users无guard”（ControlPlane198拦截）、“三处通用helper覆盖KB”（HumanBrowserPrincipal/SQL predicate另有路径）、“owner只授权读”（原话是管理员所有权限）及“恰好三条断言”。已向CC派重大节点反向挑战`58228`，不因其首份报告改变用户授权或制造生产P0/PASS。新zCode角色实现包`zcode-pdec013-role-implementation-next.md`已准备但未派发，须在方案收敛及offboarding writer释放后执行。

- PDEC-013 文档变更后结构检查 **10 passed / 0.29s**，`git diff --check` 通过，working-tree release hygiene **3193 paths passed**。这些只证明结构/发布卫生，不证明业务权限。CC `1614` 正在独立审查方案；5份固定文档指纹在任务包内，不抢改其快照。
- Kimi correction `9a17d5f…` 已完整返回；主 Codex读完最终报告并检查receipt，613条协议Internal error已如实保留，不因wrapper success升级质量。新选择事件/late audience/几何/文案/双平台截图候选待CC首审，Linux目录随作者返回释放后只交下一个reviewer独占；不把作者自测或旧13-pass当独立证据。
- 16:26 UTC 已派Session correction CC首审（`84789`，`cc-session-ui-correction-review.md`），新PDEC-013角色合同作为明确上下文，不把旧管理员private-deny当该UI切片应保留的产品门。16:30 UTC前后取回M0第二次修正并逐一验证18路径/`8267fe60…`；作者指出API单用户offboarding未获原allocation，主Codex已核实User→Pairing顺序并扩展给zCode（`23402`，`zcode-offboarding-pairing-sibling-fix.md`）。这属于已授权同根因修复，不要求owner重复批准本地文件；实际生产身份/计费权限仍未扩大。

- 2026-09-05 owner 确认管理员能读私有会话/文件/知识、凭据不明文展示。已记录 PDEC-013 并修订角色域/前端域/Runbook；当前源码仍在 `check_agent_access`、`authorize_session_action`、`users._require_org_admin`、`/agents/system/hr`、`guards.tsx` 与 workspace sections 保留冲突旧规则，故尚非功能完成。准备中的 HR 任务不再要求保留 platform-admin HR denial；新跨层切片先固定后端/测试合同再接 UI，重大节点需双向对账。此决定不是 GROVE 实际分配权限表单的 action-time 确认。

2026-09-04 已实时核对 Git、三服务 deployment、CI、manifest、源码和定向测试：RC 尚未完成，0/96 指正式双遍尚未闭环，不是没有实现功能。以下旧验证记录均是其原快照的 supporting evidence；凡与上表当前事实不同，以新实测为准，不能外推到当前 dirty candidate。

- 新执行合同文档/测试包：zCode 完成两次结构测试更新（correction receipt `03cb8526a1584d3f830bad93bb8802b5`）；CC 首次 review 已完成（receipt `2b51710568b243ee8c9adb201464ba20`，旧 terminal `58402` 已结束）。主 Codex 实测确认 F1 发布门红灯并修复，F3 全 eval 数字已校正，F2/F7 改为有日期的历史事实，F6 区分自撰核对与真正独立意见；两张顺序表增加显式对应，重大节点不把 owner 裁决的文档转录误作新决策。
- CC F4/F5 的 zCode 修正已返回（receipt `08099acf6faa43dcac94e5e4a6dc8b3a`）；CC correction review（receipt `4ea7301c00a94593be5210560978de27`，terminal `59288` 已结束）确认原 F1–F7 收口、可开始产品实现。CC 独立用 `backend/.venv` 跑 architecture **254 passed**；主 Codex 随后独立检查 zCode diff、复算 HEAD/current 冻结指纹、四处 metadata-only delta、非 ID mutation 拒绝，并重跑 **254 passed / 24.42s**、Ruff/format/diff 与 3188-path gate。存储 Goal 为 3979 字符，模板 3971；仅规范历史品牌名后逐字相同，状态 active。CC 没有 Goal 工具，以上 readback 由 Codex 完成，不伪称 CC 已验证。
- CC 新 N1（profiles.allowed_effects 可改宽而测试不红）经 Codex 内存副本 mutation 独立复现；当前 profiles 与 HEAD 相同，没有实际扩大授权。N1 与 N2/N3 最小注释修正纳入 zCode M0 包，不再另开文档等待循环；N4 已补实际 `backend/.venv` 解释器。保留不同用途的 verification_status，不新增 reviewer/controller，不把当前 NPTCR=0 写死。文档准备完成不等于 M0 或 RC 完成；尚未 commit/push 或部署。
- M0 首包 zCode delegation `437e6112807444a6a084ed63986eaee2` 已结束；Kimi delegation `b5eff751b8d547858c6828addbea1c57` 也已返回（旧 terminal `4121` 结束，duration **4493.683s**），不重复派发。前置 HR 覆盖为 `AgentCreate`/mounted preview **9 passed**、`test_hr_agent_endpoint.py` **5 passed**；现有 green 不覆盖 live platform-admin 入口反复重试的 P2，且不证明 employee creation 或 provisioning 已验收。
- HR Hindsight 页把 preview drafting 与 creation confirmation 合并成前置 gate 的措辞已依据当前源码/域合同纠正（`correction-hr-preview-drafting-precedes-exact-authenticated-creation-confirmation`）；保留 System HR canonical drafting 与 exact authenticated provisioning confirmation，不新增产品决策或控制层。
- CC M0 review 已完成（receipt `a5da5e54f1d4491ca4ed7e2205581d22`，旧 terminal `26730` 结束），18-path 起止指纹均为 `a655c666d61d521e35bed5857640fe4e08d2d14e110090e28ab9dff132cc590c`。Codex 随后独立复现 F1 子 root 真实目录替换被误删、F2 Hive clone 穿越 symlink；再补充 G1 初始化失败回滚也误删替换目录、G2 拒绝 claim 后仍遍历/读取 foreign workspace 并报 `boundary_ok=true`。所有复现均为本地 test-owned 合成文件，不含生产数据/真实凭据；G1/G2 探针为 `/tmp/hive-weekend-renewal.MqQXgw/codex-m0-review-probes.py`。原有 387 测试未覆盖这两条路径。
- Codex 对 CC 观察作了区分：F3 required `pre_state_db_absent` 须绑定真实 freshness fact；F4 是注释澄清而非新权限门；F5 不把 path check 冒称 hostile same-UID sandbox；F6 是不可用 bridge token 的 issuance 缺口，下游 live auth 已拒绝，不表述为实际越权。zCode 修正已返回，仍须 CC → Codex review，再做 M0 重大节点双向对账。
- N1 已由 CC 与 Codex 分别复算：HEAD/current `{journey_families,scoring,profiles}` 同为 `b9fc6e3b6638f3ff49f679ac0a3d70dd671fa7d4e0b4ce470f651c97362e80bd`；当前 manifest 全文件 hash 仍为 `c2bd2544…`，仅三个顶层 metadata key 与 HEAD 不同。内容 hash 支持未改 manifest 的结论；mtime 只是辅助，不能单独证明作者增量或无改动。当前 Goal 读回仍 active，不重建。
- Linux 视觉集成环境：复用已安装 `mcr.microsoft.com/playwright:v1.60.0-noble`（linux/arm64、Node 24.15.0）；在 `/tmp/hive-weekend-linux.BriX54` 仅按现有锁文件补齐 304 个依赖，宿主 frontend 依赖/版本未改。实际 Chromium `148.0.7778.0` 启动通过。该目录先用于旧 HEAD 校准，**14:57 UTC 已同步当前 frontend tracked 文件（排除 .env）和 3 个 exact untracked 候选文件**，保留 Linux node_modules；package/lock 逐一 hash 一致。CC review 期间独占该临时目录，主 Codex 不同时写入/跑测试；CI 为 Ubuntu/Node 22，不能冒称环境完全相同。
- 旧提交 Linux 校准在 `--network none` 下为 **3 failed / 10 passed（2.7m）**：首两项停在初始页面加载/Loading workspace，第三项无法观察 online→offline 的目标状态。独立空页面探针确认该容器 `navigator.onLine=false`、正常 bridge 容器为 true；无生产请求/凭据。原 trace/截图保留于该目录的 `test-results-network-none`、`playwright-report-network-none`。改为正常网络拓扑后，同一旧提交、同一测试/断言/超时/基线 **13 passed / 16.9s**（旧 terminal `98502` 结束）；支持后续 Linux 候选校准，不是 Kimi 候选或生产 PASS。初始加载的失败没有单独完成因果隔离，不笼统归因于网络。
- CC UI 首次 review `63940c7e76524d648ea38856b0107f07` 已返回（旧 `14517` 结束，1013.245s，无 protocol errors）；CC 起止及 Codex 复核 18-path 指纹均为 `2f7a12832d603b5048e7f61b02349e9165be2fb4f0d5a9e13e921837f6834d78`。F1 为 audience 晚到导致 operator 宽屏一直折叠，F2 为时间线对象重建触发自动展开；F3 为过期 Linux idle 图仅差 0.0096 而穿过 0.01 门，另四张旧图失败、两张新 expanded 图尚未到达；F4 为英文单数语法。Codex 不采纳 F1 的 P0 标签：未证明数据损坏、泄漏或全局不可用，但仍需修正。
- Codex 随后独立读取同候选 caller/diff/迁移断言与全部 7 张 Darwin 图、旧 Linux idle 图；四文件 **156 passed / 1.28s**、production build 和两项 bundle budget 通过。真实组合 hooks 的 F1/F2 与新增 G5 共 **3 个反例全红**；G5 还由 Linux Chromium 正常 UI 复现：select → collapse → 同项 explicit select 后 24 次轮询一直折叠。另 G6 实测 toggle 与首 badge **576px 重叠**，完整 viewport 也显示 expanded 首标题被 toggle 覆盖。探针源保留 `/tmp/hive-weekend-renewal.MqQXgw/codex-session-ui-review-{hook,e2e}-probes.*`；浏览器证据在 Linux 目录的 `codex-review-browser-results` 与 `codex-review-overlap-results`，CC 证据在 `/tmp/cc-review-artifacts`。没有改 root frontend 源码。
- 窄屏 expanded 的左侧 68px 缺失经 Codex browser 再验是 capture artifact：panel x0..740、document scrollWidth=innerWidth=740，而旧 screenshot target 起于 x68；完整 viewport 中面板可见。徽章 Enter 后焦点落到 collapse-toggle，几何与键盘探针通过。这不豁免独立存在的 toggle/header 重叠；720 CSS px 只称 zoom-equivalent，不冒称实际操作浏览器 zoom。修正要求完整截图、真实动作与像素门；不为坏截图重写布局。
- Kimi correction 包 `/tmp/hive-weekend-renewal.MqQXgw/kimi-session-review-corrections.md` 于 15:25 UTC 派发（delegation `9a17d5f245934de5a8a55667b00cedf0`，terminal `46928`），覆盖 F1–F4、Codex G5/G6 和完整窄屏/双平台图。Linux 临时 probe 已撤出并校验原 spec 恢复，现由 Kimi 独占该目录；Codex 不并发跑其 frontend 测试。完成后再 CC → Codex review，未提前接受。
- Kimi receipt 的 `status=success/end_turn`、`assistant_text_truncated=false`，但记录 **1453 个 `Internal error` protocol_errors**；完整报告和候选已收到，错误数量既不证明功能失败也不证明验证可靠，交 CC 独立验证，不重启作者或修改派发工具。作者提及临时 clean-HEAD/stash 对比；当前没有 stash ref，不能仅凭措辞断言全局 stash 或 owner 数据丢失。
- zCode correction `684ac7d227b34e63839b2dc6ba1e0a90` 已结束（旧 `69423`）。CC correction review `c8ed1de1d38d4e83b9e0a0bad064839f` 也已结束（旧 `79660`，930.829s，success/end_turn，无 protocol errors）；Codex 随后检查同一 18-path 指纹 `bbc15ec3d80c404ba8edce0aba86bcb2677ee479e41ad30a41732929fb6b6176`，重跑 CC 两组真实临时文件系统探针与 392 项 eval/architecture。mtime 不是未改动证明；manifest 内容 hash 仍为 `c2bd2544…`，冻结子集仍为 `b9fc6e3b…e80bd`。
- Codex 补查确认真实 PostgreSQL exchange/retirement 返回 `40P01`，不能直接接受 CC 的两 UPDATE 调序推测；另在合成 process 返回后替换 workspace，真实 adapter 仍报告 completed/boundary_ok/cleanup_verified=true，foreign marker 被复制进 scoring input。后者是 mocked process + 真实 filesystem/adapter，不是 provider/semantic score。脚本为 `/tmp/hive-weekend-renewal.MqQXgw/test_codex_m0_pairing_retirement_race.py` 与 `codex-m0-workspace-replacement-probe.py`。连同 CC 的 artifacts/scoring 父链、post-claim 读取、cleanup 声明与 real-PG negative exchange 覆盖，15:42 UTC 交 zCode 第二次修正（terminal `67632`，delegation `fbf28bd0a9a24c2d8d938a47e7f4313b`，包 `zcode-m0-second-review-corrections.md`）；只额外允许直接 identity-bootstrap 集成测试路径，不扩大 benchmark 框架。
- 下一 Kimi 切片包 `/tmp/hive-weekend-renewal.MqQXgw/kimi-entry-recovery-next.md` 与 `kimi-knowledge-novice-next.md` 仅准备、未派发，分别为已复现 HR/侧栏入口与知识库新手表达/恢复指引；先等当前 UI 完成日常 review，避免共享样式/i18n 并发写入。后者明确保留后端 retryability，不从代码推断当前生产 provider 配置。
- 个人知识库 read-only live 检查新增 `KNOWLEDGE-NOVICE-DISCLOSURE-001`：普通首屏仍含 canonical MD、taste/profile、管线等术语，media-unavailable 错误缺少重试前提说明。未触发生产上传/重试、读取 provider 配置或新模型调用。当前 UI 三文件 **27 passed / 1.36s**、后端服务 **68 passed / 0.94s**，只作已有行为基线，不证明该 UX 缺口已修。它安排为后续独立 Kimi 切片，不挤入当前 Session 或 HR/侧栏包。
- Hindsight `Conventions and patterns` 的 E/D 父子关系及历史 review 优先级已用当前 Runbook/AGENTS 核正并写入 correction：`E` 是 `D` 的 evidence-only direct child，不部署 E；历史 review 不高于当前指令和源码/运行事实。此修正不改发布合同或现有授权。
- 两个 owner 引用任务于本轮重新读回，均为已结束的历史诊断；不从旧任务恢复过时部署阻塞。当前 `secrets_provider.py`/config、admin company API 与 AdminLayout 相对 production supporting `6d46459e` 无 diff；保留历史 KDF 协议常量，不进行凭据迁移或变量变更。主 Codex 新跑 secrets/storage、admin company、invitation migration **33 passed / 14.78s**（1 条既有 Starlette deprecation warning）、AdminLayout **2 passed**，working-tree legacy gate **3191 paths passed**；不是 committed archive 或新 production PASS。
- M1 fixture 准备只做了已知临时目录/文档的有界 metadata 检查，未找到可安全复用的合成登录材料；没有全盘检索凭据或清除现有 platform-admin 会话。PDEC-008 已允许 supported-path 合成身份准备，不沿用旧 pass-2 文档的 owner gate，也不把本次未找到材料升级全局 blocker。
- 随后已用正常 UI 注册 GROVE（见合成资产登记），Chrome 保持 tenantless setup；In-app 保留 owner 登录并只填好指定实验公司的管理员分配表单，未提交。Browser action-time 确认尚未收到，自动 Goal continuation 不代替确认；仅该权限 lane 等待，zCode/Kimi 继续。续跑时标签被回收后已复原并标记 handoff，cookie 登录仍有效，没有重注册或读取真实密码。

- fresh-schema atomic suite 已完整 **15/15 passed（3.0m）**。迭代中依次修复：Goal 初始输入未走 canonical admission、terminal boundary 对 T0 relay metadata 误判、T0 UUID hex/hyphen identity 漂移、artifact trigger 的 terminal UUID 文本匹配、subagent terminal 与 durable notification 非原子、canonical `session_terminal_outcome` 绕过 Team close projection。最终整组固定顺序无失败；该证据不迁移为 production Journey PASS。
- 十个 Session/tool/authority/runtime/audit 根因已有 production `Verified` evidence；finding-level Verified 不自动升级 Journey。
- 旧 manifest hash 上的 `P29-PADMIN` clean pass 1 与 pass-2 blocker 文件只保留为历史 supporting evidence；current manifest 下 pass 1/pass 2 均未运行。缺少 employee/operator fixture 不再阻断 Agent 功能探针，只影响后续 canonical persona acceptance。
- signed-in Browser 只读复核确认 D3 Session `0731ec15…` 在生产显示 `完成 · GLM-5.3`、精确 final、一个 77 B artifact、0 running/0 waiting；它只证明真实 Session/model/tool/artifact 基础路径存在，不是当前 manifest P01 PASS。
- `RESPONSE-LEARNING-COMMIT-ORDER-001` 已形成本地 `Fix Candidate`：Kernel/invoker 不再发低层 terminal hook；Web 只在 canonical commit receipt 后调度 secret-redacted `RESPONSE_COMPLETE`；candidate/projection 按稳定 key 原子幂等。主 Codex跨域 **304 passed**，独立 review **52 passed**，Ruff/format/diff check 绿；但 postcommit crash recovery 尚未闭环，不能标 `Verified`。
- `SESSION-SUMMARY-COMMIT-ORDER-001` 已 production-shaped RED **1 failed**：实际顺序为 `summary_projection → canonical_hold`；`ChatSession.summary/last_message_at` 在 final authority 前已 commit，并可经 Session recall 回流后续 Agent。
- owner action-time 确认后，`P01-STAGE1-FRESH-FALCON-682` 只发送一次。production GLM-5.3 完成公开 3 步计划、3/3 Work Ledger todos、一次 `write_file`、一次 `read_file`、七项硬判据 final 与一个 artifact；主 Codex通过 artifact preview 独立核对标题、marker、3 行议程、`TOTAL_MINUTES=90`、2 行风险、`RISK_ROWS=2`、四项现场清单。hard reload 后 final/todos/artifact/0 running/0 waiting 全部恢复。该 principal 为 `platform_admin`，只证明功能 supporting evidence，NPTCR 不变。
- 2026-08-31 合同修订后（已被 2026-09-04 execution metadata 修订取代）的 manifest 为 `valid=true`、denominator 96、hash `7994b502361de0eafbea17b0fa5fd33eaa47d8a1381dc151126a463e2416d93a`；当时核对未新增、删除或重排旅程。当前 hash 以上表为准，旧 hash 不作为 current-manifest PASS。
- P08-J4 candidate 已把四个 seam-level P1 收口到共享 authoritative boundary：exact-session 工具/路径在 execution pipeline 与最终文件 I/O 双重校验，active exact profile 在 terminal 前锁定，provider/final/terminal evidence 绑定 exact run，authority/build identity 不完整即 fail closed；J4/Session/tool/health 合并 **169 passed**，尚未升级 Journey verdict。
- exact D3 `0ce51f04` 的 GitHub run `33528559298` 已全部成功：frontend **34/34**、atomic **15/15**、backend **8863 passed / 3 skipped**，prompt-injection、reward-hack、internal eval 与 committed legacy gate 全绿。D3 没有部署，因为随后发现并复现了 synthetic tenant 无最终 identity retirement、Local Bridge credential 未 live-check User/Tenant，以及 J4 FreeCode/Hermes 本地前置缺口；新 coherent D 必须包含这些根因修复。
- identity cleanup candidate 保留普通无 body tenant delete 的 active-account detach 语义，只对 platform admin 的显式 retirement body 执行 exact-user、Agent blocker、authority revocation、inactive identity 与 append-only replay receipt。真实 PostgreSQL 已证明并发 replay 仅一条 audit，password/JWT/refresh/bridge/WS 全失效；主 Codex独立定向 **53 passed**，admin/invite/legacy preservation backend **49 passed**、frontend **25 passed**、working-tree legacy gate **3186 paths**。
- J4 preflight candidate 的全 eval **105 passed**。临时 Case-sensitive APFS clean Hermes source `f98f5e74…` 已通过 exact worktree SHA `4311ed00…`、Python environment SHA `0e76526b…`、无网络 launcher smoke，`_j4_runtime_preflight` 返回 `blockers=[]`；未读取 credential、未调用模型、未生成 semantic score。正式 J4 仍须 action-time billing 授权。
- terminal/learning/reconciliation candidate 的历史合并回归达 **497 passed**（含真实 PostgreSQL）；Web、Session V2、trigger、business task、delegation、budget、worker、startup/orphan/reconciliation 均复用 required terminal outbox。重跑还发现并修复 delegation parent projection 脱离注入 tenant DB 的旁路，direct real-PG **21 passed**；这些结果早于当前 Budget/operator 修复，最新 working tree 仍须 full backend、frontend/build、fresh-chain 与 production crash/replay 证明。
- 最新源码在 CI 同型 `DATABASE_URL=127.0.0.1:1` fail-fast 环境下完整 backend 套件 **8786 passed / 2 skipped / 0 failed**（14:19）。此前同一套件捕获的 5 个失败来自 terminal recovery 把 BYPASS DB capability 带入动态 HookRegistry；现按既有 locator→tenant-scoped pattern 只在 BYPASS 下定位 `(tenant_id,id)`，实际 sealed/candidate recovery 在 tenant session 执行，RLS scope 回归 **17 passed**、terminal/Team 邻域 **51 passed**。两项 skip 的 exact 原因已登记；该结果只建立 local candidate，不迁移为 production Journey PASS。
- GitHub Harness CI 的既有全仓 Ruff 步骤会把 5082 条历史债务误算成本次提交失败；现与发布合同对齐为仅检查本次新增/修改/重命名 Python 路径，同时执行 `ruff check` 与 `ruff format --check`，full pytest 保留。首个 D CI 又暴露 `ruff>=0.8.0` 在 GitHub 漂移到 0.16.5、与本地验证的 0.15.12 规则集不一致；dev/CI 已精确 pin `ruff==0.15.12`。工作流结构 **3 passed**、YAML 解析、b2→candidate 230 个 Python 路径 Ruff/format、prompt/adversarial/internal eval 均绿；须以修正提交重跑 CI，失败 D 未部署。
- frontend 完整回归已绿：Vitest **161 files / 1193 tests**，Playwright **34/34**，i18n **9/9** 且双语 catalog 均为 4039 keys、全部 gap 为 0，production build 通过。E2E 收口了 server-owned plan hash 不回显、live-tail cursor、offline→online 连接态与完成态 disclosure 合同；4 张旧视觉图逐张核对后仅更新 stale baseline，桌面/窄屏/深色及 Axe 均通过。
- 首个 D 的 Linux CI 功能 E2E **29 passed**，另 5 个 active-state visual 因只更新了 Darwin 而稳定命中旧 Linux baseline（约 3%）。主 Codex逐张核对 CI expected/actual/diff，actual 与已接受的 Darwin active-state 结构一致，首跑/重试除 25 个抗锯齿像素外稳定；5 张 Linux baseline 已用 CI actual 精确更新，须随 Ruff pin 一起重跑 CI。
- 修正提交 `e6957205` 的 CI run `33448515330` 已确认 frontend **34/34** 与 atomic full-stack **15/15（2.9m）** 全绿；backend 干净 runner 的最早根因是工作流忽略既有 `uv.lock`，拉取 Testcontainers 4.15 后将旧 import 的弃用警告放大为 812 个真实 PG setup error，同时 runner 缺少产品要求的 Linux `bubblewrap`。余下 4 个 unit failure 是新 DB/extension-policy seam 未显式注入，1 个 `/private/tmp` 断言错误地在 Linux 执行。candidate 现统一使用 frozen `uv.lock`、安装并验证 `bubblewrap`、补齐 unit seam 与 OS-specific gate；CI 同型定向 **15 passed**、完整 backend **8786 passed**，仍须新提交重跑三条 CI 后才部署。
- `d5282517` 的 CI run `33451025118` 已把 frontend **34/34** 与 atomic full-stack **15/15（2.9m）** 跑绿；backend 在 Ubuntu 24.04.4 干净 runner 跑到 89% 后被 30 分钟 job timeout 取消，且 collection 顺序把 51% 唯一 `F` 精确映射为真实 `_run_command` Linux sandbox 执行测试。根因是 `bwrap --version` 只证明二进制存在，而 Ubuntu 24.04 默认以 AppArmor 限制无特权 user namespace。D4 candidate 安装官方 `apparmor-profiles`、只加载 scoped `bwrap-userns-restrict`，并调用仓库既有 `probe_os_sandbox_capability()` 证明真实 launch；不关闭全局 AppArmor、不启用 unsandboxed bypass、不跳过测试。完整 backend 本机实际需 14:19，job budget 从 30 调为 60 分钟；仍须新 CI 全绿才部署。
- coherent baseline `6d46459e` 的 CI run `33453594851` 已全部成功，且 exact frozen archive 的 backend `637818b5…`、backend-api `0bce9b71…`、frontend `5dccd5b8…` 均为 production `SUCCESS`；public health/frontend 绿。它是当前 supporting baseline，不是含 operator 修复的最终 D。
- production `WEEKEND-RC-ROLE-FIXTURE-1B4BE5D2` 已通过支持路径形成 tenant、双 org-admin、双 member 与 operator candidate；公司创建、管理员/成员邀请、join/token refresh、Back to App→Home、管理员码不可见/不可复用与 member 负向均取得 supporting evidence。普通 Agent 功能探针不要求先切换这些角色。
- `OPERATOR-AUTHORITY-001` 已在 production 复现：generic `manage` 能以任意 reason 跨用户读 Session，delegated permission GET 泄露主体，非法 company/root mutation 被静默改写。本地 candidate 将 operator inspect 变成独立 governed grant，跨 owner 只读且 reason 必填/审计，mutation 永拒，active deny 优先于 allow，revoked/expired fail closed，并把 operator-only Agent list/detail 降为最小身份壳；当前 backend/frontend/Playwright/atomic 已统一重验全绿，但尚未形成或部署新 D。
- 首个 application candidate `ee4be6a0` 的 CI run `33526446421` 在 frontend i18n inventory 首门失败：7 个新增 operator UI key 依赖源码 fallback、未进入 en/zh catalog。当前树已补齐 exact 7 keys，local i18n audit **9/9**、en/zh **4071/4071**、所有 gap 为 0，且 Vitest **1216/1216**、build/bundle、Playwright **34/34** 相称重验全绿；`ee4be6a0` 永不部署，须以新提交重跑完整 CI。
- admin company/invite candidate 已通过 backend API + migration + real non-owner RLS lifecycle **33 passed**、最新 rolling/backfill 聚焦 **19 passed** 与 frontend mounted/UI **20 passed**。production 只读盘点为 113 个历史码、105 个 active unused，105 个目标 tenant 均已有 admin，因此部署回填全部保持 `member`；empty-tenant bootstrap 才授予 `org_admin`。完整 backend 已绿，仍不等于 production PASS。
- 历史旧品牌 release hygiene working-tree gate 曾通过（3184 paths），当时结构回归 6 passed；`6d46459e` committed archive gate 当时通过（3375 paths）。历史 KDF salt、旧 env input fallback、一次性 theme-key 迁移与 LICENSE attribution 保留；该结果不替代当前候选 gate 或生产复验，2026-09-04 新实测见上表。
- task-state resolve 指向本文件；本文件只保存当前目标、事实、证据摘要、唯一下一动作和 Not Done，不再保存旧 Kimi/zCode/ACP/timeout 执行日记。
- `backend/scripts/weekend_rc_worker_gate.py` 与对应 zCode/`agent-delegate` 测试仍是未被 active RC gate 调用的 legacy compatibility artifact；本 Goal 不调用它们，也不把其结果当作当前工作流或验收证据。

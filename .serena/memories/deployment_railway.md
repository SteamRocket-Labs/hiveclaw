Railway deployment rule after 2026-07-02 eval retirement: default Hive deploy target is production/product only. Do not deploy the retired Railway eval environment as part of normal releases. Use the archive-root upload shape from repo root so Railway still sees top-level `backend/` and `frontend/`: archive `backend` into a temp `backend-root`, run `railway up --service backend --environment production --project dd959a13-19f9-497a-9704-42c310eae230 --detach -m "deploy latest backend production archive-root"`; archive `frontend` into `frontend-root`, run the matching frontend production command. Verify only production backend/frontend deployment status and public health. Eval now follows `docs/eval-system-spec.md`: no cloned Railway eval environment, no nightly behavior gate; it reads production evidence and keeps deterministic CI gates.
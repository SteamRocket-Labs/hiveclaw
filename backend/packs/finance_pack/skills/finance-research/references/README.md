# Finance Research Disabled State

The previous finance workflows are retained in the repository as design material,
but this single catalog entry is intentionally not seeded to agents by default.

Re-enable only after the backend runtime has real live data provider coverage and
the tool surface is no longer static fallback dominated.

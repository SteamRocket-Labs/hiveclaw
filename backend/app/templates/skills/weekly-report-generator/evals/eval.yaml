cases:
- name: weekly_report_markdown
  input: Create a weekly report from supplied notes.
  expected_artifacts:
  - weekly-report.md
- name: handles_missing_or_unsafe_inputs
  input: Run weekly-report-generator with incomplete inputs or unavailable provider
    configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: weekly-report-generator

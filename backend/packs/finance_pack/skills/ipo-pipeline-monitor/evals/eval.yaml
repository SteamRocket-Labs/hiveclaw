name: ipo-pipeline-monitor
cases:
- name: monitor_cross_market_ipo_pipeline
  input: Build an IPO pipeline update across US, HK, and A-share markets.
  expected_tools:
  - finance_run_workflow
  - finance_get_ipo_pipeline
  - finance_search_filings
  expected_artifacts:
  - ipo-pipeline.md
- name: handles_missing_or_unsafe_inputs
  input: Run ipo-pipeline-monitor with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content

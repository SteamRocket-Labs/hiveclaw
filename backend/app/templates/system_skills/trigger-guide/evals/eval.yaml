name: trigger-guide
cases:
  - name: schedule_one_time_reminder
    input: "Remind me tomorrow at 9am to send the report."
    expected_tools:
      - set_trigger
  - name: inspect_before_duplicate
    input: "Create a daily monitor, but one may already exist."
    expected_tools:
      - list_triggers
      - set_trigger

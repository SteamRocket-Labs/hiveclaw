# Legacy Docs Archive

> 状态：历史资料归档区。这里的文档可能包含旧计划、旧诊断、旧实现路线或已经被后续 current truth surface 取代的讨论稿。

使用规则：

- 需要历史背景时可以读。
- 不能把这里的断言直接当当前代码或生产事实。
- 涉及代码、配置、部署、测试结果时，必须重新用工具核对当前状态。
- 如果某份归档文档重新成为当前设计，应移回 `docs/` 根目录并在 `docs/README.md` 登记。


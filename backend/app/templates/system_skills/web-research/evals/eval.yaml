name: web-research
cases:
  - name: current_company_fact
    input: "Who is the current CEO of a public company?"
    expected_tools:
      - web_search
      - web_fetch
    expected_behavior:
      - cite current sources
      - use exact dates
  - name: known_url_fetch
    input: "Read this URL and summarize its API changes."
    expected_tools:
      - web_fetch

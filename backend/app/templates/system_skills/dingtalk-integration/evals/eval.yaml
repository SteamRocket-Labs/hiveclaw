name: dingtalk-integration
cases:
  - name: current_thread_reminder
    input: "In DingTalk, remind me in 30 minutes to join the meeting."
    expected_tools:
      - set_trigger
  - name: reject_arbitrary_dm
    input: "Send a DingTalk DM to Alice by name."
    expected_behavior:
      - report no proactive DingTalk outbound tool
      - do not invent a DingTalk send tool

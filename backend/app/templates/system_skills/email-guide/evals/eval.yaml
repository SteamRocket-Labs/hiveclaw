name: email-guide
cases:
  - name: send_external_report
    input: "Email the attached investor update to jane@example.com."
    expected_tools:
      - send_email
  - name: read_thread_reply
    input: "Find the latest email from legal@example.com and draft a reply."
    expected_tools:
      - read_emails
      - reply_email

task mode: create
primary deck-profile: engineering-platform
secondary gates: product-platform; strategy-leadership

required proof objects:
- System architecture map from current repo facts: frontend, backend, kernel, tool runtime, governance, memory, stores, channels.
- Runtime workflow map: entry points -> invoker -> AgentKernel -> ToolRuntimeService -> governance/preflight -> tools/side effects.
- Memory Control Plane diagram: Markdown memory pyramid plus principal/write/activation/decision/preflight/steward controls.
- Self-evolution flywheel evidence: P0-P7 completed phases and deterministic bakeoff scenarios.
- Workflow/Plan Mode map: Plan Mode handles confirmation boundary; Workflow handles deterministic execution, journal, resume, gates.
- Governance boundary map: tenant/RLS, capability policy, action preflight, checkpoints, audit, decision trace.
- Product proof matrix: surfaces, channels, office workbench, skills/MCP, subagents/workflows/deep research.
- Evidence slide: live checkout counts and source anchors.

source/asset requirements:
- Use only current repository files and memory-derived prior context that has been rechecked against live files where facts may have drifted.
- Do not invent customer count, revenue, deployment count, or performance metrics.
- Use current checkout stats collected on 2026-06-05: backend version 1.7.0; frontend version 1.7.0; 61 backend API routers; 41 models; 158 services; 64 migrations; 30 frontend API domain adapters; 107 tool decorators across 19 tool handler files.
- Use docs/self-evolution-bakeoff-report.json as a dated proof object rather than a live benchmark claim.
- Treat Workflow and Deep Research unification carefully: Workflow runtime is real in current code, but Deep Research workflow path remains gated/feature-flagged rather than production-default.

brand authenticity constraints:
- No fake partner logos, customer logos, or unverified market badges.
- Use text-based Hive wordmark only.
- Use abstract editable diagrams, not product screenshots, because the ask is a technical roadshow deck and no screenshot set was supplied.

profile-specific QA gates:
- Every slide must make a technical claim and include a proof object, diagram, metric, or source anchor.
- Connector direction and containment boundaries in architecture diagrams must be meaningful.
- No generic "AI layer / data layer" boxes without specific Hive runtime labels.
- No more than two card-grid slides; no three consecutive slides share the same macro layout.
- Render full deck to PNG and contact sheet; run layout-quality check and fix blocking overlaps.

known missing inputs:
- No audience type specified. Assumption: technical investor / partner roadshow, not a general marketing pitch.
- No brand template supplied. Assumption: create a new restrained engineering deck with editable shapes.
- No financial traction numbers supplied. Assumption: omit traction and focus on technical moat and readiness.

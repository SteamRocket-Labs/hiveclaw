cases:
- name: basic_pdf_report
  input: Render a source-attributed PDF report.
  expected_artifacts:
  - report.pdf
- name: handles_missing_or_unsafe_inputs
  input: Run pdf-generator with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: pdf-generator

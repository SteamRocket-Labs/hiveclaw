cases:
- name: basic_pitch_deck
  input: Create a five-slide investor deck.
  expected_artifacts:
  - deck.pptx
- name: handles_missing_or_unsafe_inputs
  input: Run pptx-generator with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: pptx-generator

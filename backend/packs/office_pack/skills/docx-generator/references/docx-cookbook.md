# DOCX Cookbook

Use `python-docx` for deterministic document creation and narrow edits. Keep
document generation data-driven: build sections, tables, and styles from an
explicit payload before writing the final `.docx`.

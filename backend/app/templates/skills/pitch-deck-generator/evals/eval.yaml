cases:
- name: pitch_deck_outline_and_pptx
  input: Create a Seed pitch deck from a company brief.
  expected_artifacts:
  - pitch-deck.pptx
- name: handles_missing_or_unsafe_inputs
  input: Run pitch-deck-generator with incomplete inputs or unavailable provider configuration.
  expected_behavior:
  - state the missing inputs or configuration blocker
  - do not fabricate unsupported content
name: pitch-deck-generator
